using System;

namespace lab1.CMD
{
    public class Program
    {
        [STAThread]
        static void Main()
        {
            // Для консоли с файлом
            //LogicFileCmd FileCmd = new LogicFileCmd();
            //FileCmd.ShowFormattedData(FileCmd.CoorList);

            // Для консоли без файла
            //LogicCmd cmd = new LogicCmd();
            //cmd.ShowFormattedData(cmd.CoorList);
        }
    }
}
